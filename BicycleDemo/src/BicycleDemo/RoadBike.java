package BicycleDemo;

public class RoadBike extends Bicycle { // Created derived class that extends Bicycle class that inherits Bicycle values

    private int maximumMPH;  // declared unique private int variable that belongs to RoadBike for later use.
    private int roadRating;  // declared unique private int variable that belongs to RoadBike for later use.
}
